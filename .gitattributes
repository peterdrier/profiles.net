# Normalize line endings to LF on commit, regardless of OS.
# Windows checkouts get CRLF in working tree; commits are always LF.
* text=auto eol=lf

# Binary files — don't touch
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
*.ttf binary
*.eot binary
