# Freshness Sweep Report — 2026-05-26

- **Mode:** diff (batch)
- **Previous anchor:** `967d7e9e` (set by sweep #761, 2026-05-25)
- **New anchor:** `7de0d0d2` (upstream/main HEAD)
- **Diff range:** `967d7e9e..upstream/main` — 601 files changed (343 src/, 165 tests/, 79 docs/)
- **Mechanical entries dirty:** 11 of 11 (every trigger fired; src/**/*.cs alone hit 343 files)
- **Mechanical entries updated:** 8 — `code-analysis-suppressions` and `guid-reservations` regenerated byte-identical (no change)
- **Pruned:** 14 historical husks deleted (−9,273 lines), 2 wheat migrations (Email, Campaigns), 0 inbound refs needed retargeting
- **Docs tree:** 144,747 → 135,474 lines (**−9,273, 6.40%** — between 5% soft target and 7% hard cap)
- **Editorial docs flagged for drift review:** 113 (97 marked `flag-on-change` + 4 architecture constitution docs + 12 unmarked new sections/features carried from last sweep)

---

## Updated automatically

- **dev-stats** — appended 1 daily row (table now 100 data rows); ran `generate-stats.sh`. Note: required `brew install bash` + `brew install cloc` to run on macOS (originally Windows/WinGet path-hardcoded). Already installed for future sweeps.
- **reforge-history** — refreshed `docs/reforge-history.csv` via `generate-reforge-history.sh`. Note: required `dotnet tool install -g Reforge` to run on macOS. Already installed for future sweeps.
- **about-page-packages** — version bumps only: Anthropic 12.22.0→12.23.0. All other production package versions already matched Directory.Packages.props.
- **docs-readme-index** — added Cantina Weekly Roster feature spec entry + Cantina section invariants entry. (Other 17 docs in the touched-doc list were content edits, not adds/removes.)
- **authorization-inventory** — added Cantina/Debug/EarlyEntry controllers + new `CantinaAdminOrAdmin` policy + new `CantinaAdmin` role; added `ShiftAdminController.MoveRota` VolunteerManager action-level override; removed deleted `GovernanceController.Roles`, `_AdminTopbarUserMenu.cshtml`, and `Home/Dashboard.cshtml` AnyAdminRole check; refreshed §6 AuthorizeAsync line numbers; corrected `IssuesAuthorizationHandler` resource type (`Issue` → `IssueDetail`); date → 2026-05-26.
- **controller-architecture-audit** — refreshed action-name audit for 76 controllers. New controllers documented: `CantinaController`, `EarlyEntryRosterController`, `DebugController`. New actions added on `ProfileController` (DietaryMedical), `FinanceController` (Holded*), `VolunteerTrackingController` (ExportXlsx). Corrected `TicketTransferController` action (Lookup→Confirm). Date → 2026-05-26.
- **dependency-graph** — regenerated Mermaid graph. Notable shifts: `ProfileService` is now picture-only (`IProfilePictureService`) — major fan-in collapse via #685. New nodes for Cantina, EarlyEntry, Finance (Holded), plus Shifts sub-services (Workload, GeneralAvailability, RotaCoordinatorMessage, VolunteerTrackingExport, VolunteerTrackingService). `DriveActivityMonitorService` now eagerly takes `IAuditLogService`. `UserService` fan-in grew to 53 eager (+10 since last sweep) driven by `IUserServiceRead` adoption. `AccountProvisioningService` swapped `IProfileService` for `IUserService`. `GoogleWorkspaceSync ↔ TeamResource` cycle is now half-broken (TeamResource dropped its eager edge; GSyncSvc-side lazy edge remains).
- **service-data-access-map** — full regen across 32 sections (Profiles → AuditLog, plus Cross-Section Analysis, Cache Inventory, two appendices). Final structured-flag emission was tool-rejected mid-call so the per-section flag list isn't in machine-readable form; the diff is coherent and complete (~554 inserts / 402 deletes). Section additions: Cantina, Early Entry, Finance. Major content shifts mirror the dependency-graph regen above.
- **data-model-index** — added Finance row for 5 Holded entities (`HoldedExpenseDoc`, `HoldedCategoryMap`, `HoldedSyncState`, `HoldedCreditorBalance`, `HoldedPayment`) that were missing from the entity index.

**Unchanged (regenerated, byte-identical):**
- **code-analysis-suppressions** — block content already matches `Directory.Build.props` and `tests/Directory.Build.props` `<NoWarn>` entries; no rule additions/removals/description changes.
- **guid-reservations** — the 6 deterministic GUID blocks (0000/0001/0002/0003/0010/0026) match all literal GUIDs found across Constants/ and Configurations/.

---

## Pruned

Wheat extracted into living docs, then husks deleted. Two cohorts pruned (shifts implementation + tests/budget/email-outbox); auth/governance/feedback cohort intentionally **deferred to next sweep** (see "Deferred" below).

**Migration (wheat preserved):**

- `docs/superpowers/specs/2026-03-14-email-outbox-campaigns-design.md` → `docs/sections/Email.md`: `Status = Sent` / `SentAt` records SMTP-server acceptance, *not* inbox delivery — bounce processing is out of scope. (provenance comment added)
- `docs/superpowers/specs/2026-03-14-email-outbox-campaigns-design.md` → `docs/sections/Campaigns.md`: Template substitution (`{{Code}}`, `{{Name}}`) must HTML-encode via `WebUtility.HtmlEncode` to block XSS via user-controlled recipient names; placeholder match is `StringComparison.Ordinal`. (provenance comment added)

**Refs retargeted:** none. The deleted husks' only inbound references were self-references inside their own front-matter lines. The grep for cross-document refs to the deleted basenames only surfaced the two new provenance comments above (intentional). The previous sweep's deletions left zero dangling pointers.

**Husks deleted (all chaff — task lists, code samples now in src/, data-model tables already in section invariants):**

| File | Lines | Evidence |
|------|------:|----------|
| `plans/2026-03-16-shift-management-slices-1-2.md` | 1,070 | Covered by `sections/Shifts.md` (state machine, capacity, EE freeze, NoInfoAdmin restrictions, urgency-score weights, jsonb EE capacity) + `features/shifts/shift-management.md` (acceptance criteria, urgency scoring, auth model). |
| `plans/2026-03-17-shift-slice3-noinfo-coordination.md` | 1,512 | Covered by `sections/Shifts.md` (dashboard, voluntell, range-block, ShiftDashboardAccess policy, MedicalDataViewer gating) + `features/shifts/shift-management.md` (Coordinator Dashboard panels/period mutex/caching). |
| `specs/2026-03-17-shift-slice3-noinfo-coordination-design.md` | 355 | Covered by `sections/Shifts.md` actors/negative-access. Tactical UX choice (inline-collapse-over-modal) no longer load-bearing — dashboard UI has evolved. |
| `plans/2026-03-31-shift-preference-wizard.md` | 704 | Implementation lives in src/. Wheat (mutual-exclusivity split/merge, null vs "No Preference") already in `features/shifts/shift-preference-wizard.md` verbatim. |
| `specs/2026-03-31-shift-preference-wizard-design.md` | 129 | Already migrated verbatim into `features/shifts/shift-preference-wizard.md` — design doc is a literal earlier draft of the current feature doc. |
| `plans/2026-03-22-playwright-e2e-smoke-tests.md` | 709 | Test files in `tests/e2e/` are the spec. |
| `specs/2026-03-22-playwright-e2e-smoke-tests-design.md` | 217 | File structure became `tests/e2e/`. Wheat candidates (DevAuth e2e substrate, `expectBlocked`) flagged below — no living destination exists. |
| `plans/2026-03-22-section-based-e2e-tests.md` | 934 | Reorg landed as `tests/e2e/tests/{section}.spec.ts`. |
| `specs/2026-03-22-section-based-e2e-tests-design.md` | 356 | Per-section test inventories became the on-disk spec files. |
| `plans/2026-03-23-test-pr-intelligent-selection.md` | 310 | Primary artifact is the untracked, per-machine `/test-pr` skill. The 2 committed annotations are `// @e2e:` comments in `HumanController.cs` / `VolController.cs`. |
| `specs/2026-03-23-test-pr-intelligent-selection-design.md` | 215 | Convention table + annotation precedence rule live in the skill prompt (per-machine). |
| `plans/2026-03-27-budget-phase1.md` | 2,121 | Architecture decisions captured in `sections/Budget.md` invariants + `features/budget/budget.md` ADRs + `design-rules.md` §12 (append-only audit log). Entities/configs/controllers shipped to src/. |
| `specs/2026-03-27-budget-phase1-design.md` | 184 | Superseded by `sections/Budget.md` + `features/budget/budget.md` + `sections/Finance.md` (routing). |
| `specs/2026-03-14-email-outbox-campaigns-design.md` | 461 | Covered by `sections/Email.md` + `sections/Campaigns.md` + `features/email/email-outbox.md` + `features/campaigns/campaigns.md`. Sent-vs-inbox + HTML-encode wheat migrated above. Twin plan was pruned in #761. |
| **Total** | **9,277** | |

**Deferred (not pruned this sweep, recommended for next):**

The auth/governance/feedback cohort (11 docs, ~6,056 lines) was audited but pruning is deferred. Reasons:

- `docs/plans/2026-04-03-first-class-authorization-transition.md` (187 lines) is **still in-progress** — Events Guide controllers (`EventsAdminController`, `EventsDashboardController`, `EventsExportController`, `EventsModerationController`) still use `[Authorize(Roles = RoleGroups.EventsAdminOrAdmin)]` instead of the registered `PolicyNames.EventsAdminOrAdmin`. Both `sections/Auth.md` and `authorization-inventory.md` reference this plan. Keep until Events Guide migration completes.
- The Phase 3 tombstone in that plan carries **incident-history wheat** worth preserving before deletion: commits `225ac14`, `1626098`, `bbbe508` — two cycle-crashes from service-layer authorization enforcement (DI loop: `TeamService → RoleAssignmentService → IAuthorizationService → TeamAuthorizationHandler → ITeamService`). `design-rules.md` §11 documents the rule ("services are auth-free; controllers call `IAuthorizationService.AuthorizeAsync`") but not the cycle-crash history that prevents an agent from re-proposing the pattern. Consider lifting this into §11 as a negative-space "tombstone" note before the plan is eventually pruned. (See Questions.)
- The other 9 docs in this cohort (`feedback-upgrade.md` + design, `session1-auth-google-sync.md` + design, `session3-shift-polish-legal-section.md` + design, `vol-management.md` + gaps + `vol-volunteer-management-design.md`, `profileless-accounts-rip.md`) total ~5,869 lines. The audit found their ADRs are all operationalized in section invariants and feature docs (Feedback.md, GoogleIntegration.md, Users.md, Profiles.md, Onboarding.md, LegalAndConsent.md, Shifts.md). Deferring keeps the per-sweep delta under the 7% hard cap and leaves a defensible standalone prune for the next sweep.

---

## Flagged for human review

### High-signal findings (from mechanical regeneration)

**Authorization (`authorization-inventory`):**

- **Events Guide controllers still use raw role strings.** `EventsAdminController`, `EventsDashboardController`, `EventsExportController`, `EventsModerationController` still carry `[Authorize(Roles = RoleGroups.EventsAdminOrAdmin)]` even though `PolicyNames.EventsAdminOrAdmin` is registered. Same dialect in `_Layout.cshtml` lines 111-112 / 119-120 (`User.IsInRole(EventsAdmin) || User.IsInRole(Admin)`). **Carryover from last sweep — not yet addressed.** → migrate to `[Authorize(Policy = ...)]` / `authorize-policy=` and the transition plan can then be pruned.
- `DevSeedController.ResetDashboard` uses `[Authorize(Roles = RoleNames.Admin)]` instead of `PolicyNames.AdminOnly` (behaviourally identical; dev-only, low priority — carryover).
- `ProfileController` — 18 separate per-action `IAuthorizationService.AuthorizeAsync(User, userId, UserEmailOperations.Edit)` call sites duplicate the same resource check across email-management endpoints. Candidate for a `ResolveAndAuthorizeEmailAsync(userId)` helper similar to `ContainerController.ResolveAndAuthorizeAsync`. **Carryover — now 18 sites.**

**Controllers (`controller-architecture-audit`):**

- **`ProfileController` still ~55 actions** across own-profile + email + privacy + comms-prefs + public viewing + the full human-admin surface — DietaryMedical was added this period, growing the pile. Existing Part 2 of the audit calls out proposed `HumanAdmin` / `ProfileEmail` / `ProfilePrivacy` splits; the case strengthens.
- **`FinanceController` mixes core budget CRUD with a Holded vendor integration sub-surface** (4 new `Holded*` actions this period). Worth considering a `HoldedAdminController` split before the integration grows further.
- `TeamController.Roster` (shift roster) and `.Birthdays`/`.Map` (community features) remain on a team-detail controller — carryover from existing Part 2.

**Dependency graph (`dependency-graph`):**

- `UserService` fan-in grew to **53 eager + 2 lazy** — concentrated sink, intentional post-deletion-cascade design but worth watching.
- `TeamService` fan-in **26 eager + 2 lazy** — second-largest.
- `GoogleWorkspaceSync ↔ TeamResource` cycle is now **half-broken** (TeamResource dropped its eager `IGoogleSyncService`); the GSyncSvc side still uses ad-hoc `serviceProvider.GetRequiredService<ITeamResourceService>()` in `ReconcileNobodiesDriveAsync`. Closing this cycle cleanly would let the lazy edge be removed.
- `DriveActivityMonitorService` still writes `ctx.AuditLogEntries` directly (tracked §6 baseline violation).
- `HoldedFinanceService` consumes the full `IBudgetService` — slated to narrow to `IBudgetServiceRead` per the read/write split. Comment in code already notes this.

### Editorial drift candidates (`flag-on-change`)

The diff range fired the trigger globs for essentially every editorial doc (343 src/ files changed, every section's `src/Humans.Application/Services/<Section>/**` trigger matched). Same fan-out as last sweep:

- **`docs/sections/`** (26 docs): AuditLog, Auth, Budget, Calendar, Campaigns, Camps, Cantina, CityPlanning, Containers, Email, Expenses, Feedback, Finance, GoogleIntegration, Governance, Guide, Holded, Issues, LegalAndConsent, Notifications, Onboarding, Profiles, Shifts, Store, Teams, Tickets, Users — review for drift, especially sections with the most src/ activity (Shifts, Tickets, Profiles, Finance/Holded).
- **`docs/features/`** (56 docs): all with markers fired. Prioritize where the section saw concrete additions: cantina/daily-roster (new), profiles/dietary-medical-nudge (substantial changes), shifts/workload-dashboard (substantial changes).
- **`docs/guide/`** (15 docs): user-facing guide pages — light touch unless behavior changed.
- **`docs/seed-data.md`** (1).
- **Architecture constitution docs** (4) — `flag-on-change` on `src/**` shifts fired heavily: `design-rules.md`, `conventions.md`, `coding-rules.md`, `code-review-rules.md`. Review for architectural-pattern drift (notably the half-broken `GoogleWorkspaceSync ↔ TeamResource` cycle and the further Cantina/EarlyEntry/Finance section additions to the dependency graph).

### Unmarked editorial (no freshness markers — add markers or review)

Carryover from last sweep — these still have no inline `freshness:triggers` / `flag-on-change` markers, so they are never auto-flagged. Recommended to add markers next time you touch them:

- `docs/sections/Agent.md`, `docs/sections/Debug.md`, `docs/sections/Events.md`, `docs/sections/Mailer.md`, `docs/sections/Scanner.md`, `docs/sections/admin-shell.md`
- `docs/features/26-events.md`, `docs/features/27-guide-browser.md`, `docs/features/43-google-group-membership-sync.md`, `docs/features/agent/agent-section.md`, `docs/features/scanner/scanner-barcode.md`, `docs/features/test-system-reliability.md`
- `docs/guide/AiHelper.md`, `docs/guide/EmailAccount.md`, `docs/guide/SigningIn.md`, `docs/guide/TicketTransfers.md`, `docs/guide/TwoStepVerification.md`, `docs/guide/YourData.md` (6 new guide pages added since the last sweep)

---

## Proposed for review (medium-confidence wheat — apply manually if durable)

These were surfaced by the prune subagents as borderline. The orchestrator rejected them as "not obviously durable" or "no genre-fitting destination." Apply manually next pass if you judge them durable.

1. **DevAuth-as-e2e-substrate** → `docs/features/auth/authentication.md` (new "Dev Login" subsection) or `docs/sections/Auth.md`:
   > Preview environments enable `DevAuth__Enabled=true`, exposing `/dev/login/{persona}` (volunteer, admin, board, consent-coordinator, teams-admin, camp-admin, ticket-admin, noinfo-admin, volunteer-coordinator). Playwright drives them by simple navigation — no cookie management, no OAuth, no test-double maintenance. This is the e2e substrate.

2. **`expectBlocked` four-mechanism denial-UX catalog** → no obvious destination:
   > The app denies access through four distinct UX mechanisms depending on the route: redirect-away, 404 "Page Not Found", "Access Denied" text page, `[data-status="403"]` Forbid page. Useful reference for new auth tests and for understanding why denied pages render inconsistently.

3. **Playwright `--workers=2` rationale** → comment in `tests/e2e/playwright.config.ts`:
   > More than 2 workers cause flaky timeouts on resource-constrained preview Docker hosts. Empirically learned (6→2).

4. **Standalone-Node-project isolation rationale** → extend the one-liner in `docs/features/test-system-reliability.md` § Out of scope:
   > `tests/e2e/` is deliberately *not* in `Humans.slnx`, *not* in the Docker build (`.dockerignore`), *not* deployed — separate substrate with different failure modes.

5. **`@e2e: <spec-file>` annotation convention** → comment near the existing annotations in `HumanController.cs` / `VolController.cs` (or paragraph in `docs/architecture/conventions.md`):
   > The `/test-pr` skill maps changed controllers to e2e specs via `{Controller-Suffix}.spec.ts` convention. Override with `// @e2e: <spec-file>` when the convention guesses wrong (`HumanController` → board.spec.ts + profile.spec.ts; `VolController` → shifts.spec.ts).

6. **Build-period EE capacity step-function lookup** → `docs/sections/Shifts.md` (Invariants):
   > Early-entry capacity is a step function over `dayOffset`: find the largest key in `EventSettings.EarlyEntryCapacity` ≤ dayOffset; if no key found (dayOffset is below the earliest threshold), return 0. (Currently in code at `EventSettings.GetEarlyEntryCapacityForDay`, not in section invariants.)

7. **Per-signup approval re-validation** → `docs/sections/Shifts.md` (Invariants):
   > On `ApproveAsync`, re-run overlap-check and capacity-check; return a warning to the approver if issues found. (The range-approval path's "auto-refuses pending signups for shifts that have filled since the request was placed" is documented; the per-signup `ApproveAsync` warning-not-block behavior is not.)

8. **Phase 3 service-layer-authorization tombstone — incident-history wheat** → `docs/architecture/design-rules.md` §11:
   > Two prior attempts to enforce authorization at the service layer (#418 PR #210 role assignment shipped and broke QA; #419 Google sync merged as 1626098 reverted in bbbe508) both crashed on the same DI cycle: `TeamService → RoleAssignmentService → IAuthorizationService → TeamAuthorizationHandler → ITeamService`. #420 (budget mutations) closed as "won't do" 2026-04-15. The current rule (services are auth-free; controllers call `IAuthorizationService.AuthorizeAsync`) exists specifically to prevent this regression — do not re-propose service-layer enforcement without addressing the cycle. (Captures the cycle-crash narrative before the transition plan is eventually pruned.)

---

## Questions

1. **Auth/governance/feedback cohort prune deferral confirmation.** Eleven docs (~6,056 lines) were audited and ready to prune but deferred — primarily because `docs/plans/2026-04-03-first-class-authorization-transition.md` is still active (Events Guide controllers still on the old role-based pattern). Want to:
   - (a) Migrate the Events Guide controllers to policy-based auth first, then prune the whole cohort in one PR next sweep? (recommended)
   - (b) Prune the 9 non-blocked docs now and leave the transition plan + Phase 3 tombstone for later?
   - (c) Defer entirely until Events Guide migration completes?
2. **`TeamResourceService` graph classification** (carryover from last sweep, not actioned). The dependency-graph regen kept `TeamResourceService` as a Google-Integration node (folder-based). Pin to Teams for diagram readability, or leave folder-based as-is?
3. **Should the `service-data-access-map` machine-readable flag list be reconstructed?** The agent's structured JSON return was rejected mid-call, so the per-section cross-section-violation table isn't in this report. The file diff is coherent and complete, but I don't have the violations summary in JSON form. Worth a focused re-dispatch next sweep, or read the diff manually?

---

## Skipped (errors)

- **`service-data-access-map` — final JSON return rejected (permission prompt).** The file edits completed coherently (~954-line file; 554 inserts / 402 deletes; TOC + 32 section headings + Cross-Section Analysis + Cache Inventory + 2 appendices all intact). Only the agent's structured flag-summary JSON was lost.

---

## Notes for next sweep

- **First-time setup needed on macOS** (now done): `brew install bash` (script needs bash 4+ for associative arrays), `brew install cloc`, `dotnet tool install -g Reforge`, and `git remote add upstream https://github.com/nobodies-collective/Humans.git`.
- The 14-husk prune brought the docs tree to 135,474 lines. Continued steady-state pruning of the auth/governance/feedback cohort and the deferred barrios/ticket-vendor cluster (still untouched from the 2026-05-25 sweep) is the main remaining opportunity.
